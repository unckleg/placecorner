{% extends 'AdminBundle:Master/Layouts:admin.html.twig' %}
{% set page_title = 'admin.module.region.breadcrumb.translate'|trans({}, 'admin') %}

{% block breadcrumbs %}
    <div class="page-bar">
        <ul class="page-breadcrumb">
            <li>
                <a href="{{ path('admin_dashboard') }}">
                    {{ 'admin.global.breadcrumb.home'|trans({}, 'admin') }}
                </a>
                <i class="fa fa-circle"></i>
            </li>
            <li>
                <a href="{{ path('admin_region') }}">
                    {{ 'admin.module.region.breadcrumb.region'|trans({}, 'admin') }}
                </a>
                <i class="fa fa-circle"></i>
            </li>
            <li class="active">
                <a>
                    {{ 'admin.module.region.breadcrumb.translate'|trans({}, 'admin') }}
                </a>
            </li>
        </ul>
    </div>
{% endblock %}

{% block toptitle %}
    <h1 class="page-title"> {{ 'admin.module.region.name'|trans({}, 'admin') }}
        <small> {{ 'admin.module.region.translate.new_translation'|trans({}, 'admin') }}</small>
        <div class="pull-right">
            <div class="btn-group">
                <a class="btn blue btn-circle btn-md" href="{{ path('admin_region') }}">
                    {{ 'admin.global.buttons.go_back'|trans({}, 'admin') }}
                </a>
            </div>
        </div>
    </h1>
{% endblock %}

{% block content %}
    <div class="row">
        <div class="col-md-12">
            <div id="form-wizard" class="block form-wizard">
                <ul class="form-wizard-levels">
                    {% for language in languages %}
                        <li {% if app.request.get('lang') == language.short|lower %} class="active" {% endif %}>
                            <a href="{{ path('admin_region_edit', {'id': id, lang: language.short|lower}) }}">
                                {{ language.short|upper }}
                            </a>
                        </li>
                    {% endfor %}
                </ul>
            </div>
            <div class="portlet light bordered">
                <div class="portlet-title">
                    <div class="caption font-dark">
                        <i class="icon-grid font-dark"></i>
                        <span class="caption-subject bold uppercase"> {{ 'admin.module.region.translate.caption'|trans({}, 'admin') }} </span>
                    </div>
                </div>
                <div class="portlet-body form">
                    {% include '@Admin/Region/forms/create.html.twig' %}
                </div>
            </div>
        </div>
    </div>
{% endblock %}


{% block javascripts %}
    {{ parent() }}
    {{ loadLibraries('gmaps', 'js')|raw }}
    <script src="http://maps.google.com/maps/api/js?key=AIzaSyD07QN1C5ATB6dgzP9l5kF_CDn7ihVXric"></script>

    <script>
        var unsplitedGeoLocation = '{{ region.getTranslatable().getMap() }}';
        var regionGeolocation = unsplitedGeoLocation.split(', ');
        var map = new GMaps({
            el: '#map',
            lat: regionGeolocation[0],
            lng: regionGeolocation[1],
            zoom: 7,
            click: function(event) {

                // first remove markers if exist
                map.removeMarkers();
                var lat = event.latLng.lat(),
                    lng = event.latLng.lng(),
                    coordinates = lat +', '+lng;

                map.addMarker({
                    lat: lat,
                    lng: lng,
                    title: '{{ 'admin.module.region.form.geo_map_text'|trans({}, 'admin') }}'
                });

                // setting map input coordinates
                $('#region_map').val(coordinates);
            },
        });

        map.addMarker({
            lat: regionGeolocation[0],
            lng: regionGeolocation[1],
        });
    </script>
{% endblock %}