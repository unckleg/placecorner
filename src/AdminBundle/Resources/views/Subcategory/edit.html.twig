{% extends 'AdminBundle:Master/Layouts:admin.html.twig' %}
{% set page_title = 'admin.module.subcategory.breadcrumb.edit'|trans({}, 'admin') %}

{% block breadcrumbs %}
    <div class="page-bar">
        <ul class="page-breadcrumb">
            <li>
                <a href="{{ path('admin_dashboard') }}">
                    {{ 'admin.global.breadcrumb.home'|trans({}, 'admin') }}
                </a>
                <i class="fa fa-circle"></i>
            </li>
            <li>
                <a href="{{ path('admin_category') }}">
                    {{ 'admin.module.category.breadcrumb.category'|trans({}, 'admin') }}
                </a>
                <i class="fa fa-circle"></i>
            </li>
            <li>
                <a href="{{ path('admin_subcategory', {'parentId': parentCategory.getId}) }}">
                    {{ 'admin.module.subcategory.name'|trans({}, 'admin') }}:
                    <strong> {{ parentCategory.getTitle }} </strong>
                </a>
                <i class="fa fa-circle"></i>
            </li>
            <li class="active">
                <a>
                    {{ 'admin.module.subcategory.breadcrumb.edit'|trans({}, 'admin') }}
                </a>
            </li>
        </ul>
    </div>
{% endblock %}

{% block toptitle %}
    <h1 class="page-title"> {{ 'admin.module.subcategory.name'|trans({}, 'admin') }}
        <small>
            {{ 'admin.module.subcategory.edit.pagetitle'|trans({}, 'admin') }}
            <strong> {{ category.getTitle() }}</strong>
        </small>
        <div class="pull-right">
            <div class="btn-group">
                <a class="btn blue btn-circle btn-md" href="{{ path('admin_subcategory', {'parentId': parentCategory.getId}) }}">
                    {{ 'admin.global.buttons.go_back'|trans({}, 'admin') }}
                </a>
            </div>
        </div>
    </h1>
{% endblock %}

{% block content %}
    <div class="row">
        <div class="col-md-12">
            <div id="form-wizard" class="block form-wizard">
                <ul class="form-wizard-levels">
                    {% for language in languages %}
                        <li {% if app.request.get('lang') == language.short|lower %} class="active" {% endif %}>
                            <a href="{{ path('admin_subcategory_edit', {'id': category.getTranslatable().getId(), lang: language.short|lower}) }}">
                                {{ language.short|upper }}
                            </a>
                        </li>
                    {% endfor %}
                </ul>
            </div>
            <div class="portlet light bordered">
                <div class="portlet-title">
                    <div class="caption font-dark">
                        <i class="icon-grid font-dark"></i>
                        <span class="caption-subject bold uppercase"> {{ 'admin.module.subcategory.edit.caption'|trans({}, 'admin') }} </span>
                    </div>
                </div>
                <div class="portlet-body form">
                    {% include 'AdminBundle:Subcategory/forms:create.html.twig' %}
                </div>
            </div>
        </div>
    </div>
{% endblock %}
